import 'package:flutter/material.dart';
import './ui/login.dart';

// for images create new images folder on top dir and change the pubspec.yaml



void main() {
  runApp(new MaterialApp(title: "Login Application", home: new Login(),));
}